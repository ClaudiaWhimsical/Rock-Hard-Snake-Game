/*
package org.academiadecodigo.HackStreetBoys.TeamBrian;

public class Movement {


    GameFrame.Grid grid;


    //TODO: if we want to make the snake...
    // moveRight(){ ((grid.setCol(grid.getCol)+1), grid.getRow()}
    // moveLeft(){ ((grid.setCol(grid.getCol)-1), grid.getRow()}
    // moveUp(){ ((grid.getCol(), grid.setRow((grid.getRow()-1))}
    // moveDown(){ ((grid.getCol(), grid.setRow((grid.getRow()+1))}
    //
    // TODO: This is to change the absolute location of the object.

    public void moveRight(){
        snakedemo.translate(grid.getCol)+1),0);
        System.out.println("DIREITA!");
    }

    public void moveLeft(){
        snakedemo.translate(-10,0);
        System.out.println("ESQUERDA!");
    }

    public void moveUp(){
        snakedemo.translate(0,-10);
    }

    public void moveDown(){
        snakedemo.translate(0,10);
    }
}

*/
/*  public enum GridDirection {
    UP,
    DOWN,
    LEFT,
    RIGHT;

  public boolean isOpposite(GridDirection dir) {
      return dir.equals(oppositeDirection());
  }

    public GridDirection oppositeDirection() {

  public GridDirection oppositeDirection() {

        GridDirection opposite = null;

        switch (this) {
            case UP:
                opposite = GridDirection.DOWN;
                break;
            case DOWN:
                opposite = GridDirection.UP;
                break;
            case LEFT:
                opposite = GridDirection.RIGHT;
                break;
            case RIGHT:
                opposite = GridDirection.LEFT;
                break;
        }

        return opposite;
    }
*//*



*/
